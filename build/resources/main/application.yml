spring:
  profiles:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/pi
    username: pi
    password: pi
    driverClassName: 'org.postgresql.Driver'
    sql-script-encoding: UTF-8
  jpa:
    properties:
      hibernate:
        dialect: 'org.hibernate.dialect.PostgreSQLDialect'
server:
  port : 8090